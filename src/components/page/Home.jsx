import styled from "styled-components";
import { Header } from "../ui/Header";

const Wrapper = styled.div``;

export const Home = () => {
  return (
    <Wrapper>
      <Header />
    </Wrapper>
  );
};
