// 폼 컴포넌트의 실제 모델을 담당합니다.
export const useForm = (univPorperty, departProperty, keywordsProperty) => {
  const [univ, setUniv, univRef] = univPorperty;
  const [depart, setDepart, departRef] = departProperty;
  const [keywords, setKeywords] = keywordsProperty;

  // 폼 전송
  // 입력 초기화
  // 유효성 검사
};
